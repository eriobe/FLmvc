@model List<FLmvc.Models.Person>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
       @foreach (var p in Model)
       {
           <div>@p.Firstname</div>
           <div>@Html.ActionLink("Mer information", "ShowPerson","Person",new { id = p.Id },new { @class = "min klass" })  </div>
       }
    </div>
</body>
</html>
